header.header
    .header__top
        .container  
            .contacts
                a.contact(href="tel:+380999999")
                    i(class="fa fa-mobile" aria-hidden="true")            
                    span.contact__text 999 999
                
                a.contact(href="mailto:info@business.com")
                    i(class="fa fa-envelope contact__icon contact__icon-email" aria-hidden="true") 
                    span.contact__text info@business.com           
                
                a.facebook-icon(href="facebook.com")
                    i.fa.fa-facebook(aria-hidden="true")
    
    .container
        .header__bottom
            .logo: img.log__pictuer(src='images/logo.png' alt="logo")
            nav.nav
             -var items = ['Home','About Us', 'Our Apporch', 'Our Work']
                each item in items
                    if item == 'Home'
                        a.nav__link.nav__link--activ(href="#")=item
                    else
                        a.nav__link(href="#")=item
            a.button.header__button.button--red(href="#") Free Consultation
    